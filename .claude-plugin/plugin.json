{
  "name": "figma-to-design",
  "description": "Design-aware iterative code generation from Figma to production-ready Next.js/React. Generates design tokens from your codebase, pulls Figma designs via MCP, generates code matching your project conventions, and visually verifies output via Playwright screenshot comparison — iterating until the result matches the design."
}